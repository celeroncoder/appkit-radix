import XCTest
@testable import AppKitRadixDialog

final class DialogPrimitiveTests: XCTestCase {
    func testModuleImports() {
        // Validates the module links correctly
        XCTAssertTrue(true)
    }
}
