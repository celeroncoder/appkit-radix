import AppKit
import AppKitRadixCore

/// Avatar — User image with loading state and fallback.
///
/// Radix UI equivalent: `@radix-ui/react-avatar`
public enum AvatarPrimitive {
    // MARK: - Parts
    // TODO: Root, Image, Fallback
}
