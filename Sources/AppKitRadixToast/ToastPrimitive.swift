import AppKit
import AppKitRadixCore

/// Toast — Temporary auto-dismissing notification.
///
/// Radix UI equivalent: `@radix-ui/react-toast`
public enum ToastPrimitive {
    // MARK: - Parts
    // TODO: Provider, Root, Title, Description, Action, Close, Viewport
}
